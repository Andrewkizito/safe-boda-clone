// Importing core components
import { Text } from "react-native-paper";
import Layout from "../../components/Layout";

const Home = () => {
    return (
        <Layout>
            <Text variant="bodyLarge">Good Morning, Kizito</Text>
        </Layout>
    );
};

export default Home;
